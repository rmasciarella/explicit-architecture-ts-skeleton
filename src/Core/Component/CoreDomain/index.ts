/**
 * CoreDomain - The core domain bounded context
 *
 * This module contains the core domain entities, value objects,
 * domain services, and domain events.
 */

// Export domain entities, value objects, services, etc.
// Example: export * from './Entity';
// Example: export * from './ValueObject';
// Example: export * from './Service';
// Example: export * from './Event';

export {};
